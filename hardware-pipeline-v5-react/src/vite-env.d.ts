/// <reference types="vite/client" />

// Bundle build time, baked in by Vite at build-time via `define`.
// See vite.config.ts. Used by ChatView's build-tag marker.
declare const __BUILD_TIME_IST__: string;
